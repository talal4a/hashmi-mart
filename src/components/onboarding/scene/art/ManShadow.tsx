/* eslint-disable */
// Generated from src/assets/mart-svg/02-shadows.svg by outputs/gen_scene_art.py.
// Do not hand-edit: re-run the generator instead. The viewBox is cropped to
// this layer's own bounds (105x63 at 222,343) so the wrapper can scale it about its
// own centre; the coordinates inside are untouched, so the layer still lands
// exactly where it does in the 500-unit illustration.
import { memo } from 'react';
import Svg, { Ellipse } from 'react-native-svg';

function ManShadow() {
  return (
    <Svg width="100%" height="100%" viewBox="222 343 105 63">
      <Ellipse cx={274.47} cy={373.78} rx={49.4} ry={28.52} fill="#e0e0e0" />
    </Svg>
  );
}

export default memo(ManShadow);
