// Leftover scratch file — intentionally empty. Safe to delete:
//   rm src/types/__temp_stubs.d.ts
// (It briefly held stub declarations used to typecheck the auth changes before
// the native packages were installed; the sandbox can't delete files.)
export {};
