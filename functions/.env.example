# Local emulator only. Copy to functions/.env.local and paste a real key there.
# .env.local is gitignored; production reads the GROQ_API_KEY secret from
# Google Secret Manager instead (see README, "AI Support Chat backend").
GROQ_API_KEY=gsk_replace_me
