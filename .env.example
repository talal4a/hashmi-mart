# The deployed support Worker. Public, not a secret — the Groq key lives in
# Cloudflare (see README, "AI Support Chat"). Copy this file to .env and put
# your own Worker URL in it.
EXPO_PUBLIC_SUPPORT_API_URL=https://hashmimart-support.YOUR-SUBDOMAIN.workers.dev
